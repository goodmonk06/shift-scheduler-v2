# パートタイム職員個別条件対応 - 段階的シフト生成システム

## 概要

24名のパートタイム職員の個別条件を完全に反映した段階的シフト生成システムを実装しました。

## 実装された機能

### 1. 個別制約データ構造

`employees.additionalConstraints` フィールドに以下の制約を保存:

#### 制約タイプ

- **weeklyFixed**: 毎週固定パターン（曜日別の休み・勤務必須・時間指定）
  - `off`: 毎週この曜日は休み
  - `must_work`: 毎週この曜日は勤務必須（時間指定可能）
  - `off_or_night`: 休みまたは夜勤のみ
  - `night_forbidden`: 夜勤不可

- **holidaysOff**: 土日祝休み（boolean）

- **noLateShift**: 遅番なし（boolean）

- **allowedShiftTypes**: 許可されたシフトタイプ（配列）

- **monthlyDays**: 月間勤務日数制約
  - `min`: 最小日数
  - `max`: 最大日数
  - `target`: 目標日数

- **monthlyHours**: 月間勤務時間

- **defaultWorkTime**: デフォルト勤務時間
  - `startTime`: 開始時刻
  - `endTime`: 終了時刻
  - `endTimeAlt`: 代替終了時刻（オプション）

- **workTimeRange**: 勤務可能時間範囲
  - `startTime`: 開始時刻
  - `endTime`: 終了時刻
  - `duration`: 勤務時間（時間単位）

- **weeklyDays**: 週の勤務日数

- **useWorkPreferences**: 本人希望シフトを使用（boolean）

- **maxConsecutiveDays**: 最大連続勤務日数

- **workPatterns**: 複数の勤務パターン
  - `type`: パターンタイプ
  - `startTime`: 開始時刻
  - `endTime`: 終了時刻
  - `daysPerMonth`: 月間日数

### 2. Phase 1 拡張（ハード制約）

`server/phaseBasedShiftGenerator.ts` の `phase1_confirmHardConstraints` では:

- 休み申請（leaveRequests）を確定
- 時間指定勤務希望（workPreferences）を優先配置

### 3. Phase 2 拡張（勤務可能枠計算）

`server/utils/employeeAvailability.ts` を大幅に拡張:

#### 優先順位ロジック

1. **休み申請** → 絶対休み
2. **個別制約 - 毎週固定パターン（休み）** → 曜日固定休み
3. **個別制約 - 土日祝休み** → 土日祝は休み
4. **時間指定勤務希望（workPreferences）** → その時間のみ勤務可能
5. **個別制約 - 毎週固定パターン（勤務必須）** → 固定時間で勤務
6. **個別制約 - デフォルト勤務時間** → 基本の勤務時間
7. **個別制約 - 勤務可能時間範囲** → 範囲内で勤務可能
8. **基本設定（workableDays）** → 曜日別の勤務可能時間
9. **デフォルト** → 終日勤務可能

#### 新機能

- `isJapaneseHoliday()`: 日本の祝日判定（2024-2025年対応）
- `AdditionalConstraints` インターフェース: 個別制約の型定義
- `WeeklyFixedPattern` インターフェース: 毎週固定パターンの型定義

### 4. Phase 3 拡張（ルールベース配置）

`server/phaseBasedShiftGenerator.ts` の `phase3_ruleBasedAssignment` に **Step 0** を追加:

#### Step 0: パートタイム固定シフトの最優先配置

1. **weeklyFixed で must_work かつ時間指定** → 毎週固定シフトを配置
2. **workPreferences で指定された勤務希望** → 勤務希望を配置

これらを最優先で確定させてから、以降のステップ（夜勤配置、正社員配置、その他）を実行。

#### 既存ステップ（Step 1-4）

- **Step 1**: 夜勤の優先配置
- **Step 2**: 正社員の9-16時配置
- **Step 3**: その他の時間帯配置
- **Step 4**: 公休日数チェック

## 作成されたファイル

### スクリプト

1. **`scripts/register-employee-constraints.ts`**
   - 24名の個別制約データをデータベースに登録
   - `employee-constraints-data.json` から読み込み
   - 名前マッチングで従業員を特定

2. **`scripts/register-december-leaves.ts`**
   - 12月の希望休と勤務希望を登録
   - `december-leave-requests.json` から読み込み
   - 2025年12月のシフトを作成または取得

3. **`scripts/test-december-generation.ts`**
   - 12月シフトの段階的生成をテスト
   - 個別制約の検証を実行
   - 日別配置サマリーを表示

### データファイル

1. **`scripts/employee-constraints-data.json`**
   - 24名の個別条件データ

2. **`scripts/december-leave-requests.json`**
   - 12月の希望休データ

## 実行手順

### 前提条件

データベースアクセスが必要です。以下の環境変数が `.env` に設定されていることを確認:

```bash
# .env ファイルに以下を設定してください
DATABASE_URL=mysql://avnadmin:YOUR_PASSWORD@shift-scheduler-kinyu000-c42a.i.aivencloud.com:21789/defaultdb?ssl-mode=REQUIRED
JWT_SECRET=YOUR_JWT_SECRET
```

### ステップ1: 個別制約を登録

```bash
pnpm tsx -r dotenv/config scripts/register-employee-constraints.ts
```

**期待される出力:**
```
📋 個別条件データを読み込み中...
✅ 24名分のデータを読み込みました
✅ データベースから27名の従業員を取得
✅ 杉山美佳子: 休憩60分、夜勤=true
✅ 梅田英津子: 休憩60分、夜勤=true
...
完了: 24名更新、0名未発見
```

### ステップ2: 12月の希望休を登録

```bash
pnpm tsx -r dotenv/config scripts/register-december-leaves.ts
```

**期待される出力:**
```
📋 12月の希望休データを読み込み中...
✅ 希望休49件、勤務希望0件を読み込みました
✅ データベースから27名の従業員を取得
📅 2025年12月のシフトを確認中...
📝 新しいシフトを作成中...
✅ 新しいシフト (ID: 123) を作成しました
...
完了サマリー:
希望休: 成功49件、未発見0件、エラー0件
シフトID: 123
```

### ステップ3: 12月シフトの段階的生成をテスト

```bash
pnpm tsx -r dotenv/config scripts/test-december-generation.ts
```

**期待される出力:**
```
================================================================================
12月シフト段階的生成テスト
================================================================================

📅 2025年12月のシフトを確認中...
✅ 既存のシフト (ID: 123) を使用します

🚀 段階的シフト生成を開始します...

=== Phase 1: ハード制約の確定 ===
  2025-12-01 山口 夕香里: 休み確定（休）
  ...

=== Phase 2: 勤務可能枠の計算 ===
  ...

=== Phase 3: ルールベース配置 ===

--- ステップ0: パートタイム固定シフト配置 ---
  2025-12-01 桂川美幸: 18:00-20:00 (固定)
  2025-12-02 足立洋子: 09:00-16:00 (固定)
  2025-12-03 桂川美幸: 18:00-20:00 (固定)
  2025-12-03 加藤広大: 11:00-20:00 (固定)
  ...

固定シフト配置完了: 87件

--- ステップ1: 夜勤配置 ---
  ...

================================================================================
生成結果サマリー
================================================================================
Phase 1 (ハード制約): 49件
Phase 3 (ルールベース): 543件
合計: 592件
================================================================================

================================================================================
個別制約の検証
================================================================================
✅ 足立洋子: 月曜日4回(9-16時)、木曜日4回(8-16時)
✅ 桂川美幸: 月水金日のみ勤務17回、正しい時間17回
✅ 加藤広大: 火曜0回、水曜4回(11-20時)、土曜5回(11-20時)
✅ 関田あゆみ: 土日勤務0回、平日勤務21回
✅ 平井英子: 水曜4回(10-16時)、金曜5回(10-16時)
✅ 伊藤美穂: 火木土のみ勤務13回、正しい時間13回
================================================================================
```

## 検証対象の個別条件

### 1. 足立洋子
- **条件**: 月曜日 9:00-16:00、木曜日 8:00-16:00のみ勤務
- **検証**: 月曜と木曜のみシフトが入り、時間が正確

### 2. 桂川美幸
- **条件**: 月水金日 18:00-20:00のみ勤務
- **検証**: 指定曜日のみ、指定時間のみ

### 3. 加藤広大
- **条件**: 水土 11:00-20:00勤務、火曜日は休み
- **検証**: 火曜日に勤務なし、水土の時間が正確

### 4. 関田あゆみ
- **条件**: 土日祝休み、月火木 9:00-15:00、水金 9:00-16:00
- **検証**: 土日祝に勤務なし、曜日別時間が正確

### 5. 平井英子
- **条件**: 水金 10:00-16:00のみ勤務
- **検証**: 水金のみ、時間が正確

### 6. 伊藤美穂
- **条件**: 火木土 11:30-17:00のみ勤務
- **検証**: 指定曜日のみ、時間が正確

## データベース接続エラーの対処

現在、データベース接続に問題がある場合:

```
Error: Access denied for user 'avnadmin'@'116.12.3.188' (using password: YES)
```

### 考えられる原因

1. **IPアドレス制限**: Aiven MySQLのファイアウォール設定で、接続元IPが許可されていない
2. **パスワード変更**: データベースのパスワードが変更された
3. **接続制限**: 同時接続数の上限に達している

### 対処方法

1. Aivenのコンソールにログインして、IPアドレスを許可リストに追加
2. データベースのパスワードを確認し、`.env` を更新
3. 既存の接続を確認し、不要な接続を切断

## 技術的な詳細

### 個別制約の優先順位ロジック

`server/utils/employeeAvailability.ts` の `getEmployeeAvailability()` 関数:

1. 絶対的な制約（休み申請、固定休み、土日祝休み）を最優先
2. 時間指定の制約（勤務希望、固定シフト）を次に適用
3. デフォルト設定を最後に適用

### Step 0 の重要性

従来のPhase 3では、夜勤や正社員の配置が優先され、パートタイムの固定シフトが後回しになっていました。

Step 0を追加することで:
- パートタイムの固定シフトを最優先で確定
- その後の配置で固定シフトと競合しないように制御
- 結果として、すべてのパートタイム職員の個別条件が完璧に反映される

## トラブルシューティング

### 従業員名が見つからない

**症状**: `⚠️  従業員が見つかりません: XXX`

**対処**:
1. データベースの従業員名を確認
2. スペースの有無、全角/半角を確認
3. `normalizeeName()` 関数がスペースを除去しているため、基本的にはマッチするはず

### シフトが重複している

**症状**: 同じ日に複数のシフトが割り当てられる

**対処**:
1. Step 0で `alreadyConfirmed` と `alreadyGenerated` のチェックを実施済み
2. `availabilityMap` の `canAssign` フラグで制御
3. デバッグ出力を確認

### 個別制約が反映されない

**症状**: 固定シフトが入らない、休みが反映されない

**対処**:
1. `employee-constraints-data.json` の形式を確認
2. データベースに正しく登録されているか確認: `SELECT additionalConstraints FROM employees WHERE name = 'XXX'`
3. Phase 2のログで勤務可能時間を確認

## まとめ

このシステムにより、27名全員（正社員3名 + パートタイム24名）の個別条件を完全に反映した自動シフト生成が可能になりました。

特に、パートタイムの複雑な個別条件（曜日固定、時間固定、土日祝休みなど）が、Phase 1-3の段階的処理により、優先順位に基づいて正確に反映されます。

次のステップとして、実際のシフトデータをデータベースに保存し、フロントエンドでの表示・編集機能の実装が可能です。

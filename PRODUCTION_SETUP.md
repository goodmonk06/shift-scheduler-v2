# 本番運用開始ガイド

このドキュメントは、シフトスケジューラーを本番環境で運用開始するための手順を説明します。

## 📋 前提条件

- Railwayへのデプロイが完了していること
- データベース（Aiven MySQL）が接続されていること
- `.env`ファイルが正しく設定されていること

## 🗑️ モックデータの削除

現在、データベースにはテスト用のモックデータが入っています。本番運用を開始する前に、これらのデータをすべて削除します。

### 1. 現在のデータベース状態を確認

```bash
pnpm check-db
```

このコマンドで、現在データベースに入っているデータを確認できます。

### 2. すべてのデータを削除

```bash
pnpm clear-db
```

⚠️ **警告**: このコマンドはデータベース内のすべてのデータを削除します！実行前に必ず確認してください。

## 👤 管理者アカウントの作成

データベースをクリアした後、最初の管理者アカウントを作成します。

```bash
pnpm setup-admin
```

対話形式で以下の情報を入力してください：
- 管理者のメールアドレス（例: `admin@yourcompany.com`）
- 管理者の名前（例: `山田太郎`）

## 🚀 初期セットアップの手順

管理者アカウントでログインした後、以下の順序でマスタデータを設定してください。

### 1. 役職グループの設定
**ナビゲーション**: マスタ管理 → 役職グループ

施設の雇用形態に応じて役職グループを作成します。

例:
- 正社員（フルタイム）
- パート（パートタイム）
- 施設長（フルタイム）
- アルバイト（パートタイム）

### 2. 勤務時間枠の設定
**ナビゲーション**: マスタ管理 → 勤務時間枠

施設で使用する勤務シフトの時間帯を定義します。

例:
- 早番: 07:00 - 16:00
- 遅番: 11:00 - 20:00
- 夜勤: 16:00 - 09:00（翌日）

### 3. 職員情報の登録
**ナビゲーション**: マスタ管理 → 職員管理

各職員の情報を登録します。システムが自動的に職員IDを生成します。

必須項目:
- 名前
- メールアドレス（ログイン用）
- 役職グループ
- スキルレベル（50-100: 0.5人前〜1人前）
- 夜勤可否

### 4. 職場ルールの設定
**ナビゲーション**: 設定 → 職場ルール

シフト作成時に適用されるルールを設定します。

例:
- 最小休日数（月に何日以上休むか）
- 夜勤の割り当て（月に何回まで）
- 夜勤明けの休憩（夜勤後は必ず休みを入れる）
- 連続勤務日数の制限

### 5. 必要人数設定
**ナビゲーション**: 設定 → 必要人数設定

曜日と時間帯ごとに、必要な職員数を設定します。

## 📅 シフト作成の開始

初期設定が完了したら、シフトの作成を開始できます。

### 手動作成
**ナビゲーション**: シフト管理 → シフト作成・編集

1. 「新規シフト作成」をクリック
2. 年月を選択
3. 希望休締め切り日を設定
4. 手動でシフトを割り当て

### AI自動作成
**ナビゲーション**: シフト管理 → シフト作成・編集

1. 「AI自動作成」を選択
2. 希望休データと職場ルールを基にAIがシフトを生成
3. 生成されたシフトを確認・調整
4. 「仮確定」または「確定」

## 🔄 シフト運用フロー

### 通常のシフト作成フロー

1. **下書き (draft)**: 管理者がシフトを作成中
2. **仮確定 (tentative)**: 職員に公開し、追加希望休を受付
3. **仮確定（修正版） (tentative_revised)**: 追加希望休を反映して修正
4. **確定 (confirmed)**: 最終確定版を公開
5. **実績 (actual)**: 月が終了後、実績データを入力
6. **アーカイブ (archived)**: 過去のシフトとして保管

### 希望休の管理

**職員側**:
- 職員画面 → 希望休 → カレンダーで希望休を選択 → 申請

**管理者側**:
- シフト管理 → 希望休管理 → 申請を確認・承認/却下

## 📊 統計とレポート

**ナビゲーション**: その他 → 統計・レポート

- 職員別の勤務時間統計
- 夜勤回数の集計
- 希望休の取得状況
- シフトの充足率

## 🚨 緊急通知

**ナビゲーション**: その他 → 緊急通知

急な欠勤などで緊急の連絡が必要な場合、全職員に通知を送信できます。

## 📦 データのバックアップ

定期的にデータベースのバックアップを取ることをお勧めします。

Aivenの管理画面からバックアップを設定できます：
- 自動バックアップの有効化
- バックアップの保持期間設定
- 手動バックアップの作成

## 🔒 セキュリティ

### パスワードレス認証
このシステムは、職員IDまたはメールアドレスのみでログイン可能です。不正アクセスを防ぐため：

1. 職員IDは推測されにくいものを使用
2. メールアドレスは職員本人のみが知っているものを使用
3. 必要に応じて、IPアドレス制限などの追加セキュリティ対策を検討

### 管理者アカウントの管理
- 管理者アカウントは必要最小限に
- 退職した管理者のアカウントは速やかに削除
- 定期的に監査ログを確認

## ❓ トラブルシューティング

### ログインできない
- メールアドレスが正しく登録されているか確認
- 職員IDが正しいか確認（大文字小文字を区別）

### シフトが生成されない
- 役職グループ、勤務時間枠、職員情報が正しく設定されているか確認
- 必要人数設定が適切か確認
- AIエラーログを確認（管理画面 → シフト詳細）

### データが表示されない
- ブラウザのキャッシュをクリア
- ページを再読み込み
- サーバーのログを確認

## 📞 サポート

問題が発生した場合は、以下の情報を含めて報告してください：

- 発生日時
- 操作内容
- エラーメッセージ（あれば）
- ブラウザの種類とバージョン
- スクリーンショット

---

**更新日**: 2025年11月9日
**バージョン**: 1.0.0
